import React from 'react';
import { Button } from 'react-toolbox/lib/button';
import theme from './TopRightButton.scss';

const SuccessTopRightButton = (props) => <Button {...props} theme={theme} />;
export default SuccessTopRightButton;
